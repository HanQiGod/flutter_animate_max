name: flutter_animate_max
description: Flutter 升级 3.35.7 之后，对 flutter_animate 插件的升级（After Flutter upgrade 3.35.7, the upgrade of flutter_animate plugins.）。
version: 4.6.0
homepage: https://github.com/HanQiGod/animated_text_kit_plus
topics:
 - animation
 - ui
 - effects
 - widgets
 - widget

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
